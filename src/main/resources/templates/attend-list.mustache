<!DOCTYPE html>
<html>
<head>
    <title>출석 게시판</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        @font-face {
            font-family: 'CookieRun';
            src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2001@1.0/CookieRun.woff') format('woff');
            font-weight: normal;
            font-style: normal;
        }

        body {
            font-family: 'CookieRun', sans-serif;
            background-color: #F8F4EF;
            color: #5C4033;
            padding: 30px;
            text-align: center;
        }
        .container {
            max-width: 900px;
            margin: 0 auto;
            background-color: #EADCD1;
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: left;
        }
        h1 {
            color: #5C4033;
            font-weight: 700;
            margin-bottom: 30px;
            border-bottom: 2px solid #D6B2A0;
            padding-bottom: 10px;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 30px;
            background-color: #FFF;
            border-radius: 8px;
            overflow: hidden;
        }
        th, td {
            border: 1px solid #B59A8F;
            padding: 12px;
            text-align: center;
            font-size: 0.95rem;
        }
        th {
            background-color: #D6B2A0;
            color: white;
            font-weight: 700;
        }

        /* 다중 삭제 버튼 영역 스타일 */
        .delete-action-bar {
            margin-bottom: 15px;
            text-align: right;
        }
        .btn-delete-bulk {
            background-color: #A08070;
            border: none;
            color: white;
            padding: 8px 15px;
            border-radius: 5px;
            transition: background-color 0.2s;
            font-family: 'CookieRun', sans-serif;
            font-size: 0.95rem;
        }
        .btn-delete-bulk:hover {
            background-color: #7A635B;
        }
        /* 페이지네이션 스타일 */
        .pagination-container {
            display: flex;
            justify-content: center;
            margin-top: 25px;
            list-style: none;
            padding: 0;
        }
        .pagination-container li {
            margin: 0 3px;
        }
        .pagination-container .page-link {
            color: #5C4033;
            font-family: 'CookieRun', sans-serif;
            border: 1px solid #D6B2A0;
            padding: 8px 15px;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.2s, color 0.2s;
            display: block;
        }
        .pagination-container .page-link:hover {
            background-color: #D6B2A0;
            color: white;
        }
        .pagination-container .active .page-link {
            background-color: #5C4033;
            color: white;
            border-color: #5C4033;
            font-weight: 700;
            pointer-events: none;
        }
        .pagination-container .disabled .page-link {
            color: #B59A8F;
            background-color: #EADCD1;
            border-color: #EADCD1;
            cursor: default;
        }
        /* 링크 그룹 스타일 */
        .link-group {
            margin-top: 20px;
        }
        .link-group a {
            color: #7A635B;
            text-decoration: none;
            margin-right: 15px;
            transition: color 0.2s;
        }
        .link-group a:hover {
            color: #5C4033;
            font-weight: 700;
        }
    </style>
</head>
<body>
<div class="container">
    <h1>출석 댓글 게시판</h1>

    <form method="POST" action="/attend/bulk-delete" onsubmit="return confirm('선택된 기록들을 정말로 삭제하시겠습니까?');">
        <input type="hidden" name="selectIds" value="">

        <div class="delete-action-bar">
            <button type="submit" class="btn-delete-bulk">선택 항목 삭제</button>
        </div>

        <table>
            <thead>
            <tr>
                <th><input type="checkbox" id="checkAll"></th>
                <th>ID</th>
                <th>사용자 이름</th>
                <th>내용</th>
                <th>기록 시간</th>
                <th>관리</th>
            </tr>
            </thead>
            <tbody>
            {{#attendList}}
                <tr>
                    <td>
                        <input type="checkbox" name="selectIds" value="{{id}}" class="rowCheckbox">
                    </td>
                    <td>{{id}}</td>
                    <td>{{username}}</td>
                    <td>{{content}}</td>
                    <td>{{formattedTimestamp}}</td>
                    <td></td>
                </tr>
            {{/attendList}}
            </tbody>
        </table>

    </form>
    <nav class="pagination-container" aria-label="Page navigation">
        <ul class="pagination-container">

            <li class="page-item {{#pageInfo.first}}disabled{{/pageInfo.first}}">
                <a class="page-link"
                   href="/attend-list?page={{pageInfo.previousPageNumber}}&size={{pageInfo.size}}&sort=timestamp,desc"
                aria-label="Previous">
                &laquo;
                </a>
            </li>

            {{#pageInfo.pageNumbers}}
                <li class="page-item {{#current}}active{{/current}}">
                    <a class="page-link"
                       href="/attend-list?page={{value}}&size={{pageInfo.size}}&sort=timestamp,desc">
                    {{displayValue}}
                    </a>
                </li>
            {{/pageInfo.pageNumbers}}

            <li class="page-item {{#pageInfo.last}}disabled{{/pageInfo.last}}">
                <a class="page-link"
                   href="/attend-list?page={{pageInfo.nextPageNumber}}&size={{pageInfo.size}}&sort=timestamp,desc"
                aria-label="Next">
                &raquo;
                </a>
            </li>
        </ul>
    </nav>
    <div class="link-group">
        <a href="/attend/new">✍️ 새 기록 등록하기</a>
        <a href="/index">🏠 홈으로 돌아가기</a>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
    document.addEventListener('DOMContentLoaded', function() {
    const checkAll = document.getElementById('checkAll');
    const rowCheckboxes = document.querySelectorAll('.rowCheckbox');
    const form = document.querySelector('form');
    const selectIdsInput = document.querySelector('[name="selectIds"]');

    if (checkAll && rowCheckboxes.length > 0) {
        checkAll.addEventListener('change', function() {
            rowCheckboxes.forEach(checkbox => {
                checkbox.checked = checkAll.checked;
            });
        });

        rowCheckboxes.forEach(checkbox => {
            checkbox.addEventListener('change', function() {
                const allChecked = Array.from(rowCheckboxes).every(cb => cb.checked);
                checkAll.checked = allChecked;
            });
        });
    }

    // 선택된 체크박스의 id 값을 hidden input에 넣는 코드 추가
    form.addEventListener('submit', function(event) {
        const selectedIds = [];
        rowCheckboxes.forEach(checkbox => {
            if (checkbox.checked) {
                selectedIds.push(checkbox.value);
            }
        });

        if (selectedIds.length === 0) {
            event.preventDefault(); // 폼 제출을 막음
            alert("선택된 항목이 없습니다"); // 경고 메시지
            return false;
        }

        selectIdsInput.value = selectedIds.join(',');
    });
});


</script>
</body>
</html>