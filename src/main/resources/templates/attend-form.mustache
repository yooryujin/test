<!DOCTYPE html>
<html>
<head>
    <title>새 출석 기록 등록</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        /* ⭐️ 쿠키런 폰트 정의 (CDN) ⭐️ */
        @font-face {
            font-family: 'CookieRun';
            src: url('https://cdn.jsdelivr.net/gh/projectnoonnu/noonfonts_2001@1.0/CookieRun.woff') format('woff');
            font-weight: normal;
            font-style: normal;
        }

        body {
            /* ⭐️ 쿠키런 폰트 최우선 적용 ⭐️ */
            font-family: 'CookieRun', sans-serif;
            background-color: #F8F4EF; /* 프로젝트 공통 배경색 */
            color: #5C4033;
            padding: 30px;
            display: flex;
            justify-content: center;
        }
        .container {
            max-width: 500px;
            background-color: #EADCD1; /* 프로젝트 공통 카드 배경색 */
            padding: 40px;
            border-radius: 15px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            text-align: left;
        }
        h1 {
            color: #5C4033;
            font-weight: 700;
            margin-bottom: 30px;
            border-bottom: 2px solid #D6B2A0;
            padding-bottom: 10px;
        }
        .btn-submit {
            background-color: #D6B2A0; /* 프로젝트 공통 버튼 색상 */
            border: none;
            color: white;
            padding: 8px 20px;
            border-radius: 5px;
        }
        .btn-submit:hover {
            background-color: #B59A8F;
            color: white;
        }
        .btn-back {
            color: #7A635B;
            text-decoration: none;
            margin-left: 15px;
            display: inline-block; /* 버튼 옆에 나란히 오도록 */
        }
    </style>
</head>
<body>
<div class="container">
    <h1>새 출석 기록 등록</h1>

    <form method="POST" action="/attend/create">
        <div class="mb-3">
            <label for="username" class="form-label">사용자 이름/닉네임</label>
            <input type="text" class="form-control" id="username" name="username" placeholder="이름을 입력하세요" required>
        </div>
        <div class="mb-3">
            <label for="content" class="form-label">출석 메시지</label>
            <textarea class="form-control" id="content" name="content" rows="3" placeholder="출석했습니다! 또는 질문 등을 남겨주세요." required></textarea>
        </div>

        <button type="submit" class="btn btn-submit">기록 등록</button>

        <a href="/attend-list" class="btn-back">목록으로 돌아가기</a>
    </form>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>